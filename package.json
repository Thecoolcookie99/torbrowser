{
  "name": "tor-cloudflare-gateway",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "wrangler pages dev public --local",
    "deploy": "wrangler pages deploy public",
    "mock:socks": "node scripts/mock-socks-server.mjs",
    "test": "tsx --test src/**/*.test.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250821.0",
    "@types/node": "^24.0.10",
    "tsx": "^4.19.2",
    "typescript": "^5.5.4",
    "wrangler": "^3.101.0"
  },
  "dependencies": {
    "tor-js": "file:vendor/tor-js"
  }
}
