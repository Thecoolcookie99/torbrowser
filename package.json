{
  "name": "onion-browser",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build --outDir dist --emptyOutDir",
    "preview": "vite preview"
  },
  "dependencies": {
    "tor-js": "^0.4.1"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
